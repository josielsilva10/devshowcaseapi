# Desenvolvimento local (SQLite)
DATABASE_URL="file:./dev.db"
PORT=3333
NODE_ENV=development

# Produção: configure DATABASE_URL no Render com a conexão interna do PostgreSQL.
# Nunca coloque a URL real de produção neste arquivo.
